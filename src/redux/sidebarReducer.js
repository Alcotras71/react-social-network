let initialState = {
  friends: [
    {id: 1, name: 'Andrey'},
    {id: 2, name: 'Saga'},
    {id: 3, name: 'Redux'},
  ]
};

const sidebarReducer = (state = initialState, action) => {

  return state;
}

export default sidebarReducer;